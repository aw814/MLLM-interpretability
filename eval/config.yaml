data:
  csv_path: "../data/processed/eclektic_long_subset.csv"
  max_examples: 50

eval:
  source_lang: "en"
  target_lang: "he"

models:
  tested_model: "gemini-2.0-flash"
  judge_model:  "gemini-2.5-flash"

decode:
  temperature: 1
  max_tokens: 128

artifacts_dir: "./artifacts"