data:
  csv_path: "../data/processed/eclektic_long_subset.csv"
  max_examples: 50

eval:
  source_lang: "en"
  target_lang: ["he", "de", "es", "hi", "id", "it", "ja", "ko", "pt", "fr"]
  # target_lang: ["zh"]
models:
  tested_model: "gemini-2.0-flash"
  # tested_model: "gpt-4o"
  judge_model:  "gpt-5"

decode:
  temperature: 1
  max_tokens: 128

artifacts_dir: "./artifacts"
