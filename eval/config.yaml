data:
  name: "eclektic"
  # name: "klar"
  max_examples: 50

eval:
  source_lang: "en"
  # target_lang: ["he", "de", "es", "hi", "id", "it", "ja", "ko", "pt", "fr"]
  # target_lang: ["zh"]
  target_lang: ["en"]
models:
  tested_model: "gemini-2.0-flash"
  # tested_model: "gpt-4o"


decode:
  temperature: 1
  max_tokens: 128

artifacts_dir: "./artifacts"
